(() => { const apply=()=>{const plan=document.querySelector('#plan');if(!plan)return;plan.querySelectorAll('.plan-plant,.garden-object').forEach(el=>{const y=parseFloat(el.style.top)||50;const depth=(y-5)/90;const scale=.72+depth*.42;el.style.setProperty('--depth-scale',scale.toFixed(2));el.style.setProperty('--depth-shadow',`${Math.round(3+depth*9)}px ${Math.round(2+depth*8)}px ${Math.round(6+depth*12)}px`);el.style.zIndex=String(Math.round(10+y))})};window.addEventListener('load',apply);new MutationObserver(apply).observe(document.body,{childList:true,subtree:true});setInterval(apply,500)})();
