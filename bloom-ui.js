(() => { const boot=()=>{const all=window.gardenowoCatalog;if(!all)return;document.querySelectorAll('#libraryList .library-item').forEach(item=>{const p=all.find(x=>x.id===item.dataset.add);const meta=item.querySelector('.library-copy span');if(p&&meta&&!meta.dataset.bloom){meta.textContent+=` · kwitnienie: ${p.bloom}`;meta.dataset.bloom='1'}})};window.addEventListener('load',boot);new MutationObserver(boot).observe(document.body,{childList:true,subtree:true})})();
