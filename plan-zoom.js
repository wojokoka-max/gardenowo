(() => { const boot=()=>{const plan=document.querySelector('#plan'),toolbar=document.querySelector('.plan-toolbar');if(!plan||!toolbar)return;let zoom=1;const box=document.createElement('div');box.className='plan-zoom';box.innerHTML='<button type="button" data-zoom="out" aria-label="Pomniejsz plan">−</button><span>100%</span><button type="button" data-zoom="in" aria-label="Powiększ plan">+</button><button type="button" data-zoom="reset">Reset</button>';toolbar.appendChild(box);const update=()=>{plan.style.setProperty('--plan-zoom',zoom);box.querySelector('span').textContent=`${Math.round(zoom*100)}%`};box.addEventListener('click',e=>{const b=e.target.closest('[data-zoom]');if(!b)return;if(b.dataset.zoom==='in')zoom=Math.min(1.8,zoom+.1);if(b.dataset.zoom==='out')zoom=Math.max(.7,zoom-.1);if(b.dataset.zoom==='reset')zoom=1;update()});update()};window.addEventListener('load',boot)})();
