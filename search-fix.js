(() => { const boot=()=>{const input=document.querySelector('#plantSearch'),list=document.querySelector('#libraryList'),all=window.gardenowoCatalog;if(!input||!list||!all)return;const draw=()=>{const q=input.value.trim().toLocaleLowerCase('pl-PL');const found=all.filter(p=>!q||p.name.toLocaleLowerCase('pl-PL').includes(q)||p.latin.toLocaleLowerCase('pl-PL').includes(q)).slice(0,80);list.innerHTML=found.map(p=>`<button type="button" class="library-item" data-add="${p.id}"><span class="plant-swatch" style="background-color:${p.color}">${p.emoji}</span><span class="library-copy"><strong>${p.name}</strong><span>${p.category} · ${p.height} · kwitnienie: ${p.bloom}</span></span><input class="plant-qty" type="number" min="1" max="99" value="1" aria-label="Liczba roślin"><span class="add-plant add-label">Dodaj</span></button>`).join('')||'<p class="helper">Nie znaleziono tej rośliny. Sprawdź pisownię albo wpisz fragment nazwy.</p>'};input.addEventListener('input',draw);input.addEventListener('change',draw);draw()};window.addEventListener('load',boot)})();
