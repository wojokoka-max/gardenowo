(() => { const boot=()=>{const plan=document.querySelector('#plan');if(!plan)return;const enhance=()=>plan.querySelectorAll('.garden-object').forEach(el=>{if(el.querySelector('.object-tools'))return;const id=el.dataset.id,o=(JSON.parse(localStorage.getItem('gardenowo-objects')||'[]')).find(x=>x.id===id);if(!o)return;o.w=o.w||1;o.h=o.h||1;o.r=o.r||0;el.style.width=`${o.w*70}px`;el.style.height=`${o.h*42}px`;el.style.transform=`translate(-50%,-50%) rotate(${o.r}deg)`;const tools=document.createElement('span');tools.className='object-tools';tools.innerHTML='<button class="object-rotate" title="Obróć">↻</button><button class="object-resize" title="Zmień rozmiar">↗</button>';el.appendChild(tools);tools.querySelector('.object-rotate').addEventListener('click',e=>{e.stopPropagation();o.r=(o.r+15)%360;el.style.transform=`translate(-50%,-50%) rotate(${o.r}deg)`;localStorage.setItem('gardenowo-objects',JSON.stringify(JSON.parse(localStorage.getItem('gardenowo-objects')||'[]').map(x=>x.id===id?o:x)))});tools.querySelector('.object-resize').addEventListener('pointerdown',e=>{e.preventDefault();e.stopPropagation();const startX=e.clientX,startY=e.clientY,w=o.w,h=o.h;const move=ev=>{o.w=Math.max(.5,Math.min(4,w+(ev.clientX-startX)/70));o.h=Math.max(.5,Math.min(4,h+(ev.clientY-startY)/42));el.style.width=`${o.w*70}px`;el.style.height=`${o.h*42}px`};const up=()=>{const list=JSON.parse(localStorage.getItem('gardenowo-objects')||'[]').map(x=>x.id===id?o:x);localStorage.setItem('gardenowo-objects',JSON.stringify(list));window.removeEventListener('pointermove',move);window.removeEventListener('pointerup',up)};window.addEventListener('pointermove',move);window.addEventListener('pointerup',up)});});new MutationObserver(enhance).observe(plan,{childList:true});enhance()};window.addEventListener('load',boot)})();
